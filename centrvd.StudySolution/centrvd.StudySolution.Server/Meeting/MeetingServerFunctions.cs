using System;
using System.Collections.Generic;
using System.Linq;
using Sungero.Core;
using Sungero.CoreEntities;
using centrvd.StudySolution.Meeting;

namespace centrvd.StudySolution.Server
{
  partial class MeetingFunctions
  {
    public void SendNotificationBySimpleTask(string text)
    {

      //var performers = _obj.Members.ToList().Select( m => m.Member).AsEnumerable();
//      var performers = new List<IRecipient>();
//      foreach (var element in collection)
//      {
//        
//      }
//      var task = Sungero.Workflow.SimpleTasks.CreateWithNotices( centrvd.StudySolution.Meetings.Resources.NotificationOfMeetingChangeTitleFormat(_obj.Name), performers, _obj);
//      task.ActiveText = text;
//      task.Save();
//      task.Start();
    }
  }
}